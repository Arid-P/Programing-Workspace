{
	// Place your Python workspace snippets here. Each snippet is defined under a snippet name and has a scope, prefix, body and 
	// description. Add comma separated ids of the languages where the snippet is applicable in the scope field. If scope 
	// is left empty or omitted, the snippet gets applied to all languages. The prefix is what is 
	// used to trigger the snippet and the body will be expanded and inserted. Possible variables are: 
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. 
	// Placeholders with the same ids are connected.
	// Example:
	// "Print to console": {
	// 	"scope": "javascript,typescript",
	// 	"prefix": "log",
	// 	"body": [
	// 		"console.log('$1');",
	// 		"$2"
	// 	],
	// 	"description": "Log output to console"
	// }
    
    "Define Python Function": {
		"scope" : "python",
        "prefix": "func",
        "body": [
            "def ${1:function_name}(${2:args}):",
            "    \"\"\"${3:Docstring}\"\"\"",
            "    ${4:pass}"
        ],
        "description": "Creates a Python function template"
    },

	"Define Starting main function": {
		"scope" : "python",
        "prefix": "main",
        "body": [
            "def main () -> None :",
            "    \"\"\"It is the starting function. From it you can understand a basic flow of the whole program\"\"\"",
            "    ${4:pass}",
			"",
			"if __name__ == '__main__' :",
			"    main() "
        ],
        "description": "Creates a Python main template"
    },

	"Import Libraies nessarcy for debugging": {
		"scope" : "python",
        "prefix": "debug",
        "body": [
            "from icecream import ic ",
            "",
        ],
        "description": "Imports icecream module"
    },

    "Establishing a Connection in sqlite3": {
        "scope" : "python",
        "prefix": "connection",
        "body": [
            "import sqlite3 as sq",
            "",
            "${1:DB_path} = r'${2:Python +SQL\\Basics (sqllite3)\\Practice databases\\school.db}'",
            "",
            "def get_connection(db_path=${1:DB_path}) :",
            "   conn = sq.connect(db_path)",
            "   cursor = conn.cursor()",
            "   return conn, cursor",
            ""
        ],
        "description": "It imports sqlite3, and then creates a function that connects to the desired database and rewturns a tuple of the connection and the cursor for that database"
    },

    "Importing sqlalchemy": {
        "scope" : "python",
        "prefix": "sqlal",
        "body": [
            "from sqlalchemy import create_engine, Column, Integer, String",
            "from sqlalchemy.orm import declarative_base, sessionmaker",
            "",
            "DB_PATH = r'Python +SQL\\Practice databases\\orm_demo.db'",
            "engine = create_engine(f'sqlite:///{DB_PATH}', echo=True)",
            "Base = declarative_base()",
            ""
        ],
        "description": "It imports sqlalchemy and otherr neccessary objects, and theen specifics the databse path, establishes the engine and declares the base class"
    }

}